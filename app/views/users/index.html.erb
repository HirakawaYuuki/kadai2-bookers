<%= render partial: 'books/list',locals: {book: @book,user: current_user}  %>

<div class="col-lg-9">
	<h2>Users</h2>
	<table class="table">
		<thead>
			<tr>
				<th>image</th>
				<th>name</th>
				<th colspan="3"></th>
			</tr>
		</thead>
		<tbody>
			<% @users.each do |user| %>
			<tr>
				<td> <%= attachment_image_tag @user, :profile_image,class: "user_image",　format: 'jpeg',fallback: "no_image.jpg", size: "100x100" %> </td>
				<td><%= user.name %></td>
				<td><%= link_to "show", user_path(user.id),class: "user_#{user.id}" %></td>
			</tr>
			<% end %>
		</tbody>
	</table>